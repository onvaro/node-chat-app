extends layout

block content
	script
		var socket = io.connect('http://localhost:3000');
		socket.on('chat', function (data) {
			console.log(data);
			socket.emit('chat response', {my: 'chat response to you'});
		});